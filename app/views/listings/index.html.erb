<% content_for :title, "Listings" %>

<div class="w-full">
  <h1 class="font-bold text-4xl my-4"><%= I18n.t 'listings.properties.title' %></h1>

  <%= render "buttons" %>

  <%= render "table" %>

  <%= render "filter" %>

</div>
