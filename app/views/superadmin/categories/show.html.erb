<% content_for :title do %>
  <%= I18n.t('superadmin.categories.index.title') %>
<% end %>

<% content_for :breadcrumb do %>
  <li><%= link_to I18n.t('superadmin.categories.index.title'), superadmin_categories_path %></li>
  <li><%= @category.id %></li>
<% end %>

<div class="w-full">
  <h1 class="font-bold text-4xl my-4"><%= I18n.t('superadmin.categories.index.title') %></h1>
  <h3 class="font-bold text-xl mb-4"><%= I18n.t('superadmin.categories.show.identifier', id: @category.id) %></h3>
  <%= render "buttons" %>
  <%= render @category %>
</div>
