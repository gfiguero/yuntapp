<div class="w-full">
  <h1 class="font-bold text-4xl my-4"><%= I18n.t('views.title.verification') %></h1>
  <h3 class="font-bold text-xl mb-4"><%= I18n.t('persona.subtitle.verify_identity') %></h3>

  <div class="alert alert-info mb-4">
    <p><%= I18n.t('persona.help.verification_info') %></p>
  </div>

  <div class="flex flex-row gap-2 mb-4">
    <%= link_to raw(icon('back') + I18n.t('actions.back')), panel_root_path, class: "btn btn-soft" %>
  </div>

  <%= render "form", persona: @persona %>
</div>
