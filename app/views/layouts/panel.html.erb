<!DOCTYPE html>
<html data-theme="light">
  <head>
    <title><%= content_for(:title) || "Yuntapp Panel" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render "shared/flash" %>
    <div class="drawer lg:drawer-open">
      <input id="my-drawer-2" type="checkbox" class="drawer-toggle" />
      <div class="drawer-content flex flex-col items-center justify-start">
        <!-- Page content here -->
        <div class="w-full navbar bg-base-100 lg:hidden shadow-sm mb-4">
            <div class="flex-none">
                <label for="my-drawer-2" class="btn btn-square btn-ghost drawer-button lg:hidden">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="inline-block w-6 h-6 stroke-current"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path></svg>
                </label>
            </div>
            <div class="flex-1 px-2 mx-2 font-bold text-xl">Panel de Usuario</div>
        </div>

        <main class="container mx-auto mt-2 mb-5 px-5 flex flex-col w-full">
            <div class="flex justify-between items-center mb-4">
                <h1 class="text-2xl font-bold">Panel de Usuario</h1>
            </div>
            <%= yield %>
        </main>
      
      </div> 
      <div class="drawer-side h-full">
        <label for="my-drawer-2" aria-label="close sidebar" class="drawer-overlay"></label> 
        <ul class="menu p-4 w-80 min-h-full bg-base-200 text-base-content h-full">
          <!-- Sidebar content here -->
          <li class="mb-4 text-xl font-bold px-4">Yuntapp Panel</li>
          <li><%= link_to "Dashboard", panel_root_path, class: ("active" if current_page?(panel_root_path)) %></li>
          
          <li class="menu-title mt-4">Mis Cosas</li>
          <li><%= link_to "Mi Perfil", panel_profile_path, class: ("active" if controller_name == "profile") %></li>
          <li>
            <%= link_to panel_verification_path, class: ("active" if controller_name == "verification") do %>
              Mi Identidad
              <% if current_user.verified_identity&.pending_verification? %>
                <span class="badge badge-warning badge-sm"><%= I18n.t('activerecord.attributes.verified_identity/verification_status.pending') %></span>
              <% elsif current_user.verified_identity&.verified? %>
                <span class="badge badge-success badge-sm"><%= I18n.t('activerecord.attributes.verified_identity/verification_status.verified') %></span>
              <% end %>
            <% end %>
          </li>
          <li><%= link_to "Mi Domicilio", new_panel_household_unit_path, class: ("active" if controller_name == "household_units") %></li>
          <li><%= link_to "Acreditación", panel_accreditation_path, class: ("active" if controller_name == "accreditations") %></li>
          <% if current_user.household_admin? %>
            <li><%= link_to "Socios del Domicilio", panel_members_path, class: ("active" if controller_name == "members") %></li>
          <% end %>

          <% if current_user.household_admin? %>
            <li><%= link_to "Certificados", panel_residence_certificates_path, class: ("active" if controller_name == "residence_certificates") %></li>
          <% end %>

          <li class="menu-title mt-4">Sistema</li>
          <li><%= link_to "Reiniciar Onboarding", panel_onboarding_restart_path, data: { turbo_method: :delete, turbo_confirm: "¿Estás seguro? Esto eliminará tu progreso actual y podrás registrarte en otra junta." }, class: "text-warning" %></li>
          <li><%= link_to I18n.t('actions.reset'), panel_reset_account_path, data: { turbo_method: :delete, turbo_confirm: I18n.t('panel.account_resets.confirm') }, class: "text-error" %></li>
          <li><%= link_to "Cerrar Sesión", destroy_user_session_path, data: { turbo_method: :delete } %></li>
        </ul>
      
      </div>
    </div>
    <%= yield :filter_drawer %>
  </body>
</html>
