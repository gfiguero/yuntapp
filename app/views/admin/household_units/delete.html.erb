<% content_for :title do %>
  <%= I18n.t('views.title.household_units') %>
<% end %>

<% content_for :breadcrumb do %>
  <li><%= link_to I18n.t('views.title.household_units'), admin_household_units_path %></li>
  <li><%= link_to @household_unit.id, admin_household_unit_path(@household_unit) %></li>
  <li><%= I18n.t('actions.delete') %></li>
<% end %>

<div class="w-full">
  <h1 class="font-bold text-4xl my-4"><%= I18n.t('views.title.household_units') %></h1>
  <h3 class="font-bold text-xl mb-4"><%= I18n.t('views.identifier.household_unit', id: @household_unit.id) %></h3>
  <%= render "buttons" %>
  <%= render @household_unit %>
</div>
