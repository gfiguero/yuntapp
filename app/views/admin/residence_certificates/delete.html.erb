<% content_for :title do %>
  <%= I18n.t('admin.residence_certificates.index.title') %>
<% end %>

<% content_for :breadcrumb do %>
  <li><%= link_to I18n.t('admin.residence_certificates.index.title'), admin_residence_certificates_path %></li>
  <li><%= link_to @residence_certificate.id, admin_residence_certificate_path(@residence_certificate) %></li>
  <li><%= I18n.t('actions.delete') %></li>
<% end %>

<div class="w-full">
  <h1 class="font-bold text-4xl my-4"><%= I18n.t('admin.residence_certificates.index.title') %></h1>
  <h3 class="font-bold text-xl mb-4"><%= I18n.t('admin.residence_certificates.show.identifier', id: @residence_certificate.id) %></h3>
  <%= render "buttons" %>
  <%= render @residence_certificate %>
</div>
