<% content_for :title do %>
  <%= I18n.t('views.title.listings') %>
<% end %>

<% content_for :breadcrumb do %>
  <li><%= link_to I18n.t('views.title.listings'), admin_listings_path %></li>
  <li><%= @listing.id %></li>
<% end %>

<div class="w-full">
  <h1 class="font-bold text-4xl my-4"><%= I18n.t('views.title.listings') %></h1>
  <h3 class="font-bold text-xl mb-4"><%= I18n.t('views.identifier.listing', id: @listing.id) %></h3>
  <%= render "buttons" %>
  <%= render @listing %>
</div>
