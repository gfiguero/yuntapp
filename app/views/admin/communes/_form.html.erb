<%= form_with(model: [:admin, commune], class: "grid gap-4 w-1/3") do |form| %>

  <% invalid = commune.errors.include?(:name) %>
  <%= form.label :name, I18n.t('attributes.commune.name'), class: "floating-label" do %>
    <%= form.text_field :name, class: "input w-full validator #{'input-error' if invalid}", required: true, placeholder: I18n.t('attributes.commune.name') %>
    <span><%= I18n.t('attributes.commune.name') %></span>
  <% end %>
  <%= raw(error_message(invalid, commune.errors.full_messages_for(:name))) %>

  <%= form.submit I18n.t('actions.save'), class: "btn btn-success" %>
<% end %>
