# Files in the config/locales directory are used for internationalization and
# are automatically loaded by Rails. If you want to use locales other than
# English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t "hello"
#
# In views, this is aliased to just `t`:
#
#     <%= t("hello") %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more about the API, please read the Rails Internationalization guide
# at https://guides.rubyonrails.org/i18n.html.
#
# Be aware that YAML interprets the following case-insensitive strings as
# booleans: `true`, `false`, `on`, `off`, `yes`, `no`. Therefore, these strings
# must be quoted to be interpreted as strings. For example:
#
#     en:
#       "yes": yup
#       enabled: "ON"

en:
  hello: "Hello world"
  verifications:
    show:
      status:
        reverted_title: Invalid certificate
        reverted_description: This certificate is no longer valid.
        reverted_badge: Not valid
  panel:
    residence_certificates:
      show:
        payment_in_review: "Your payment is under review by MercadoPago. Once approved, the certificate will be issued automatically."
        payment_reverted: "The payment for this certificate was reverted, so the document is no longer valid. If you think this is a mistake, contact your neighborhood association."
    listing_subscriptions:
      new:
        title: Enable auto-renewal
        email_label: Your MercadoPago account email
        email_hint: "It must match your MercadoPago account email, or the charge will be rejected."
        submit: Continue to MercadoPago
      flash:
        invalid_email: Enter a valid email address.
        subscription_failed: "The subscription couldn't be started. Check that the email (%{email}) matches your MercadoPago account."
  activerecord:
    attributes:
      neighborhood_association:
        rut: "Tax ID (RUT)"
    errors:
      models:
        residence_certificate:
          base:
            immutable: cannot be modified once issued
  admin:
    onboarding_reviews:
      flash:
        rejection_reason_required: "You must provide a reason for the rejection."
    dependent_reviews:
      flash:
        family_group_missing_admin: "The family group has no active household admin; the dependent cannot be approved."
        rejection_reason_required: "You must provide a reason for the rejection."
  payment_reversal_mailer:
    staff_alert:
      subject: "Alert: reverted payment in Yuntapp"
      body_intro: "A payment was reverted (refund or chargeback). Review for possible fraud."
      folio: "Certificate folio"
      listing: "Listing"
      payment_status: "Payment status"
      payment_id: "MercadoPago payment id"
      body_action: "The document was automatically invalidated. If warranted, consider deactivating the member (BR-091)."
